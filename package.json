{
  "name": "whoispete.com",
  "version": "1.0.0",
  "description": "Development environment for my personal web page.",
  "main": "dev.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "browser-sync start --server devroot --files * --watch",
    "deploy-dev": "cd devroot && scp -rp . pete@pwx.me:/var/www/wip/ && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.3"
  }
}
